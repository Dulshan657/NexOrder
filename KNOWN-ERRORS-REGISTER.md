# Known errors register

Defects found in this codebase, what happened to each, and — for the ones still
open — what it costs and why it has not been fixed.

**Scope.** Opened 2026-08-18 during the scan-gun work, so it is still
scanning-heavy. It is meant to outlive that: add to it whenever a real defect is
found, whether or not it gets fixed the same day.

**State, 2026-08-19.** Every row that could be closed is closed. The two left
open are both deliberate refusals rather than backlog — O2 is a property of
Android text input and O3 is the rule that stops the global scan capture
corrupting typing — so an empty-looking Open table would be the wrong picture,
not a better one.

**Rules for entries.**
- One row per defect, with the **symptom an operator would report**, not just the
  code smell. A register nobody can match against a bug report is useless.
- Cite the file and, where it helps, the line.
- An entry only leaves the OPEN table when the fix is **live**, not when it is
  committed.
- "Won't fix" is a legitimate outcome, but it must say *why*, and what the
  operator should do instead.

**Severity**
| | |
|---|---|
| **S1** | Silent wrong data, or a privileged action fires that nobody asked for |
| **S2** | A workflow is blocked or unusable |
| **S3** | Works, but wastes the operator's time or misleads them |
| **S4** | Cosmetic, or a latent trap with no current symptom |

---

## Open

| # | Sev | Defect | Symptom | Where | Why still open |
|---|---|---|---|---|---|
| O2 | S3 | **Global stray-scan capture cannot work under CipherLab `Input Method` mode.** An Android IME types into the focused editable and nowhere else, so with nothing focused no characters exist to listen for. | On a device left in the factory default, scanning works only after tapping the box. | `lib/scan/useWedgeScanner.ts` (documented in-file) | **Won't fix — not fixable.** It is a property of Android text input. Mitigation: use `Key Event` mode. Your RS35 is already set that way. **Since the stray channel was wired, a scan this cannot catch is at least audible everywhere else** — see F26. Under Input Method with nothing focused there is still nothing to hear, because there are still no characters. |
| O3 | S3 | **A scan fired while focus sits in some *other* input lands in that input.** The global capture stands down whenever focus is in any editable. | Scan a bin while the cursor is in a quantity box → the code is typed into the quantity box. | `lib/scan/useWedgeScanner.ts` | **Won't fix by design.** Catching it means `preventDefault` on characters not yet classified as a scan, which risks eating genuine keystrokes. The stand-down rule is the entire reason this cannot corrupt typing or double-commit. Mitigated by ScanField's refocus. **Partly mitigated as of F26:** on a screen with no scanning surface the code now gets the stray tone and a toast instead of silence. A scan aimed at *another input* is still typed into it, and still deliberately so. |

### Environment, not code

Rows stay here once resolved, with what was done — the same rule the code tables
follow. A demo that drifted back is much easier to recognise when the original
entry is still readable.

| # | Defect | Symptom | Fix |
|---|---|---|---|
| E1 | ~~**Both demo Warehouse accounts have `home_warehouse_id = NULL`**~~, and `StocktakePage` gates its Post button on that matching the selected site. | Log in as Warehouse on the demo → everything works until Post, which is disabled. (It does say why — *"This is not your home warehouse, so counts here are read-only for your account"* — which the original entry missed.) | **DONE 2026-08-18.** Both accounts homed at MAIN. Now guarded: `tests/e2e/mobile/stocktake.spec.ts` asserts the read-only warning is absent and the sheet's controls are live, as the Warehouse role, on every run. |
| E2 | ~~**The demo has 0 open replenishment tasks**~~ and only 3 armed configs. | Nothing to walk on the Replenishment queue. | **PARTLY DONE 2026-08-18.** One open task now stands on WIE-DEMO (the queue badge shows it), raised by putting `min_qty` above what the pick face holds. The armed count is still 3, and that is not fixable by arming more — see **E3**. The row's original advice ("arm min/max, then Detect") is only good advice on a site with pick-zone levels. |
| E3 | **MAIN cannot raise a replenishment task at all, and its Setup grid will refuse every row.** All 127 of its `product_home_bins` sit on bins with `level_role IS NULL`, and 166 of its 167 stock-holding locations have no level role either — so there is no pick zone to refill and no reserve to pull from. | Follow E2's old advice on MAIN — arm min/max, run Detect — and every save is refused with *"Replenishment refills a pick zone, so the home bin has to be a pick-zone level."* Verified against the deployed function 2026-08-18. | Not a bug. MAIN is seeded flat. Replenishment is demonstrable on **WIE-DEMO**, whose rack is levelled pick/reserve; `supabase/exercise-replen-gate.mjs --env=dev` drives the whole loop. To demo it on MAIN, its racks have to be levelled first. |

---

## Fixed

Newest first. Every one of these is live on the demo.

| # | Sev | Defect | Symptom before | Fix |
|---|---|---|---|---|
| F35 | S4 | **Every `backdrop-blur` in the app is 8 px, including seven on `sticky`/`fixed` chrome and on every `ProductCard` badge** — and nobody had ever measured what that costs. Was **O14**. Tailwind v4 renumbered the scale, so `blur-sm` IS 8 px and bare `blur` is a compat alias for the same figure; the codebase reads as though it uses a cheap 4 px blur and does not. | Scroll-frame repaint cost on a low-end SoC, everywhere scroll chrome is translucent. Never reported by anyone. The register carried it as "unmeasured" from the day it opened, and the harness written for it could not produce a number — the browser-automation tab is always `document.hidden`, where `requestAnimationFrame` is throttled to ~1 fps and every frame time is scheduler noise. | **Measured 2026-08-19, and the answer is "not worth changing" — with the numbers.** `tests/perf/backdropBlur.spec.ts` (headed Playwright, backgrounding disabled, `Emulation.setCPUThrottlingRate` at 6×, 360 px, nine rotated trials on the Shop with 160 blurred elements on screen); procedure and pre-registered rule in `docs/runbooks/measure-backdrop-blur.md`. Paired medians against *no blur*: **with a GPU, 8 px costs +10.7 ms/s of compositor time (+9.1 %, sign agreeing 8/9) and +0.00 ms of p95 frame interval** — real, repeatable, below the pre-registered 10 % threshold, and perceptually nil. **The single most useful finding is not the verdict: on GPU, 4 px costs the SAME as 8 px** (+10.9 vs +10.7 ms/s), so the expense is having a `backdrop-filter` layer at all rather than its radius, and F23's `-sm`→`-xs` class of fix buys nothing on hardware. If the blur ever must go, it has to *go*, not shrink. **Software rasterisation tells a completely different story — +102 ms/s, +69.2 %, 9/9** — and that is exactly why the compositing mode is recorded beside every figure: same code, same machine, same day, an order of magnitude apart, one Chromium flag between them. The GPU row is the one that describes the device (an RS35 is an Android handheld and has one); software raster is a deliberately harsher proxy, not a neutral one. **Two harness defects had to be fixed before any of it meant anything, and each produced a confident wrong answer first:** frame times were being sampled INSIDE the CDP trace (p95 of 1.7 SECONDS — a measurement of the tracer), and the estimator differenced per-condition medians while the conditions ran in a fixed order and the baseline drifted 4× mid-run. Now: a separate untraced pass for the perceptual metric, condition order **rotated** by trial index, and a **median of within-trial pairs** with a ≥75 % sign-agreement requirement — a change that can only make "worth changing" harder to reach. Still a desktop proxy: this is Chromium on Windows with a throttle multiplier, not an RS35 on adb |
| F34 | S3 | **Session persistence over a full shift had never been verified.** The `navigatorLock` fix re-enabled `persistSession`/`autoRefreshToken`, and nothing had ever confirmed the ~1-hour token actually renews. Was **O6**. **The readout built to answer it was itself wrong**, in the window the answer lands in: `sessionHealthFormat.ts` claimed supabase renews "roughly ten minutes" before expiry and therefore called anything under 50 minutes "too soon to tell". | Operator logged out mid-shift; scan picking becomes unusable — the exact symptom the lock fix was for. And the instrument would have reported *"none yet, and one was due"* for a session behaving perfectly, anywhere between 50 and 58.5 minutes in. | **Verified 2026-08-19, on a repeatable twelve-minute test rather than a ninety-minute wait.** The real margin is `AUTO_REFRESH_TICK_THRESHOLD (3) × AUTO_REFRESH_TICK_DURATION_MS (30s)` = **90 seconds**, so how long a soak takes is set by ONE project setting. `scripts/session-soak.mjs` (`npm run soak:session:dev`) drops dev's `jwt_exp` to 300s behind `requireDevTarget` — the three-guard entry point, because a shortened token lifetime on a client's system would be a real security-posture change made by a test — runs `tests/soak/sessionRefresh.spec.ts`, and restores it in a `finally`, on SIGINT, and by re-reading. `jwt_exp` is now also declared in `apply-auth-config.mjs`, so `auth:config:check` reports a value left behind by a run killed outright. **Result: three refreshes at +211s, +421s and +632s against a 300s token** — a 210s cadence, which is `lifetime − 90s` to the second and is itself the proof the ten-minute figure was wrong. The access token changed, `signedInAt` never reset (a reset would mean a silent sign-out-and-back-in, the failure wearing the costume of a pass), and a data-bearing view rendered afterwards. `refreshHint` now derives "was one due" from `expiresAt` against the real margin instead of from session age against 50 minutes — which also makes the panel correct at the 300s lifetime the soak runs at, where the old rule would have said "too soon to tell" for the entire run. **What is still not proven: the device.** This is Chromium on Windows, not a CipherLab RS35, and not Android's tab-discard behaviour. It proves the mechanism — lock, refresh, rotation, persistence — over repeated cycles, which is what `inProcessLock.ts` put at risk |
| F33 | S3 | **The site selector ran 53 px off a 360 px screen** on Stocktake, Putaway and Replenishment. Registered as **O15**, and found by F30's suite on its first run — which is the entire argument for having built it. | The picker was 267 px wide starting at x=146 (after F8's ☰ clearance), so its right edge sat at 413 on a 360 px viewport and the page scrolled sideways. Worse, it degraded with every warehouse added, because the culprit is whichever site has the longest name — today `E2E RackLevels Test (E2ERACKLVL)`. Invisible at desktop width, which is exactly why F8's clearance fix looked complete and was not. | **A native `<select>` sizes itself to its widest OPTION, not to its box** — that is the whole defect, and no amount of container padding fixes it. `min-w-0 max-w-full truncate` on the select, `min-w-0 max-w-full` on the label. **`min-w-0` is load-bearing:** without it the flex item refuses to shrink below its content and `max-w-full` does nothing at all. `ReceiveStockView` is untouched and was never affected — its destination select is already `w-full` inside a grid cell, which is why the empty-receipt spec passed while the other three failed |
| F32 | S4 | **`BAR_WIDTH_REDUCTION_PT = 0`** — ink-spread compensation was wired into the renderer, documented, and pinned at zero pending evidence. Was **O12**. | The blocker was never the number: setting one meant editing a constant and deploying an Edge Function, so the only people who could record what a printer did were the people who could deploy. The calibration ladder that produces the figure has existed since Code 128 shipped, with nowhere to write the answer down. | Per-site data (mig `00110`, `warehouse_print_calibration`) written by `mutate-warehouse` `set_print_calibration`, entered **in millimetres** beside the ladder that produces it, with `docs/runbooks/calibrate-label-bar-width.md`. **Its own table, not a column on `warehouse_label_prefs`:** ink spread belongs to the printer, and that table is keyed per `sheet_group` because three genuinely different die-cuts sit behind it — one site would have held three disagreeing answers about one device. **The CHECK is a bound on typing; the load-bearing guard is `effectiveBarWidthReduction` in `sizing.ts`,** because the narrowest bar in a Code 128 symbol is ONE module and an unclamped reduction does not narrow a symbol, it erases it. The clamp needs the module width, which depends on the code and the stock — precisely what a constant could never know. Measured on the deployed demo: 0.05 mm requested removed **0.0501 mm**; the ceiling clamped to exactly 25 % of a 1.22234 pt module leaving 75 % of the bar; clearing restored the original width **exactly**. The calibration ladder itself still prints at nominal widths with no compensation, deliberately — printed through the correction it would be measuring the correction. **The constant is still 0 everywhere until someone runs the runbook**; this made the answer settable, it did not invent one |
| F31 | S3 | **Replenishment ledger legs were not attributable to their task.** `inv_transfer_stock` wrote `ref_type='transfer'`, `ref_id=NULL` on both legs. Was **O13**. | "Which task moved this stock?" could not be answered from `inventory_movements` at all — you had to correlate on product/from/to/qty/time and hope no two tasks overlapped. | Mig `00109`: `inv_transfer_stock` gains `p_ref_type`/`p_ref_id` **whose defaults reproduce the old values exactly**, so putaway, reslot and quarantine are unchanged by construction. `wie_complete_replen_tx` stamps `('replen_task', v_completed)`. **`v_completed`, not `p_task_id`** — they are the same row on a full completion, but a partial leaves the original holding the remainder and inserts the row carrying what actually moved, and the leg must name the row whose quantity is in the leg. **The arity change (7 → 9) is why the migration DROPs first:** `CREATE OR REPLACE` at a different arity creates an overload, which has already bitten this exact function once. Verified live on the demo — a fresh cycle wrote both legs as `replen_task`/`8` against an accepted task of qty 18, while a `transfer-stock` round-trip in the same session still wrote `('transfer', NULL)` |
| F30 | S4 | **No small-screen regression coverage at all** — the only Playwright project was `devices['Desktop Chrome']`. Was **O7**. | Every fix the register lists at 360 px — F8, F15, F16, F17, F24, F25 — could regress with nothing to catch it. | A `mobile` project at 360 px running as the **Warehouse** role (`tests/e2e/mobile/`), four specs each naming the row it guards. The desktop project `testIgnore`s them: a collapsed card tier is false at desktop width *by design*, so running both would fail for the correct behaviour. **O7's stated blocker was itself stale** — `playwright.config.ts` still said "there is nowhere to run this", written when the demo was Amadiya's database and never updated after the 2026-08-13 rebuild; so was the `persistSession: false` claim in the same header and in `fixtures/auth.ts`. The no-`storageState` conclusion survives for a *better* reason: a snapshot pins one access token and this app now rotates refresh tokens. **It earned its keep immediately** — first run against the demo, 4 real failures, one of them F33 above. Two specs skip with a stated reason: the F16/F17 stop cards are in the walk view, which a read-mostly suite will not enter because starting a walk assigns work |
| F25 | **S2** | **Receive Stock’s line table was ~1100 px wide** — `w-28` + 3×`w-40` + `w-44` + `w-20` + `w-10` = 888 px of fixed columns plus an unbounded Product column. No card layout at any width. | On the RS35 (≈360 px) the receipt table was ~3× the screen, scrolled sideways; the batch-barcode box and the Hold checkbox were off to the right. | The `<table>` is gone. One render, a CSS grid template swap, and `display: contents` above the breakpoint so the collapsible tier flattens back into row cells — no second tree to keep in step. Row extracted to `components/inventory/receive/ReceiveLineCard.tsx`. **The breakpoint is `xl`, and only a browser said so:** at `lg` the product column computes to **0 px** and the row overflows by 8 px; at 1100 px it is 52 px, present and useless; 1180 px is the first width that reads. 904 px of columns "fits" in 1024 right up until you subtract 32 px of padding and 112 px of gaps. Re-measured after: zero overflow, no clipped name, 360–1920 px. Also fixes a sub-44 px control the register never listed — the batch barcode used ScanField `compact`, which drops the touch target **and** suppresses the camera button, on a scan surface |
| F26 | S4 | **First scan after a page load was silent**, and **a scan on any screen without a scanning surface was silent forever**. | No beep on the first scan of a session. Worse, and unlisted: `useWedgeScanner` is opt-in and only three surfaces opt in, so everywhere else a scan fired with nothing focused vanished with no beep, no error and no trace — the exact failure the wedge work claimed to have ended, fixed on three screens out of forty. | Both were **already-written code that nothing called**. `primeScanAudio` had shipped exported, documented and tested, with a docstring saying to call it from a pointerdown/keydown listener; now it is, in `Root`. `playScanStray` had shipped with its own third voice, tones, vibration pattern and tests, and had never once sounded; `StrayScanListener` now mounts app-wide and stands down via a module-level registry whenever a real surface claims capture — a registry and not a context, because the check has to be synchronous inside a keydown handler. **The register was wrong to call this autoplay abuse:** resuming a suspended AudioContext inside a genuine gesture, playing nothing, is the sanctioned unlock |
| F27 | **S2** | **Tailwind v4 requires Chrome ≥ 111**; Vite’s JS target was ~107, so the JS floor sat *below* the CSS floor. | "The app looks completely broken" on a managed device — it parsed, ran, and rendered with no styles at all. The scanner gets blamed first. It cost an afternoon once. | `build.target` pinned to `chrome111/edge111/safari16.4/firefox128`. **That is only half the fix and must never be shipped alone:** with the target raised the bundle no longer parses below the floor, so the symptom becomes a white screen, which says even less. `public/browser-check.js` + a plain-HTML notice in `index.html` is the other half — external because `script-src 'self'` has no `'unsafe-inline'`, ES5 because it runs on browsers that cannot parse the bundle, and testing **two** features because `color-mix()` draws the Chrome line while `@property` draws the Firefox/Safari one |
| F28 | S4 | **`QUIET_ZONE_MODULES = 10` was defined twice** — deliberately, so the encoder and the geometry need not import each other, but nothing stopped them drifting. | A mismatch has no error to read: every label still renders, passes every other test, and simply scans worse. | One equality assertion in `__tests__/labelSheet.test.ts`, which already imported both modules. The decoupling is untouched. Verified to fail when the two constants are made to disagree — a drift guard that cannot bite is worse than none |
| F22 | S4 | **`startOffset` bounds disagreed**: the UI caps at 47, the server accepts 0–64. 47 was sized for a 24-up sheet. **Two** inputs carried it, the second being `LayoutLabelJobModal` — the default print path — which the original row missed. | The row said "cannot start past 47 on a denser sheet", which is one preset. The far commoner failure was the reverse: 47 is three to six times too HIGH for `a4-14` and `a4-8`, the two stocks the system defaults to, so typing 20 onto a 14-up sheet was silently clamped to 13 by `layoutLabels` and the operator found out at the printer. `max` was decorative either way — neither input clamped a typed value, so 200 reached the server and came back as "Invalid label request". | `maxStartOffset(preset)` and a derived `MAX_START_OFFSET` in `_shared/labelSheet.ts`; both inputs bound, clamp on both sides, pull the value down when the ceiling drops, and name the ceiling in their helper text. Also threads `warehouseId` through `LayoutLabelBadge`, without which the job modal sized sheets from built-in defaults while the server applied the site's `warehouse_label_prefs` |
| F23 | S4 | **`backdrop-blur` on a `sticky` element** repaints on every scroll frame. | Slightly janky scrolling on a low-end SoC like the RS35's. **Measured 2026-08-19 (F35), and the honest reading is that this fix bought very little:** on GPU a 4 px backdrop-filter costs the same as an 8 px one (+10.9 vs +10.7 ms/s of compositor time), because the expense is having the layer at all rather than its radius. The `transform-gpu` half is untouched by that. Left in place — it is not wrong, and reverting a shipped no-op to prove a point is worse than keeping it — but do not reach for `-xs` again expecting a saving. | `backdrop-blur-xs` (4 px) plus `transform-gpu`. **Note the trap:** the obvious fix, `-sm`, is a no-op — Tailwind v4's `blur-sm` is 8 px, identical to bare `blur`. Caught only by reading `backdropFilter` off the deployed build; both computed `blur(8px)`. `transform-gpu` is belt-and-braces, since backdrop-filter already forces its own layer — `components/inventory/stocktake/CountSheet.tsx` |
| F24 | S4 | **`CountSheet`'s sticky action bar had no `flex-wrap`**, unlike the header two blocks above it. | At 360 px the summary text and the Clear/Post buttons share 328 px; the Post label wraps inside its own button. Cramped, not broken. | `flex-wrap` on the bar **and `ml-auto` on the button group instead of `justify-between`** — `justify-content` applies per flex LINE, so wrapping alone would have dropped the buttons hard-left on row two. Same shape as `ReplenSetupView`'s sticky bar |
| F1 | **S1** | **`Permissions-Policy: camera=()` denied the camera to our own origin.** An empty allowlist is a denial, not "unrestricted". | Camera scanning was dead on **every deployed build**, on every device. Worse: `isCameraScanAvailable()` only checks the API exists — which the header does not remove — so the button rendered, the sheet opened, and the operator was told to "allow camera access for this site", which is unactionable because a header is not a prompt. | `camera=(self)` — `vercel.ts` |
| F2 | **S1** | **`ReleaseQuarantineModal` used a bare `<input>` inside a `Modal` whose footer submits.** | A wedge gun's terminating Enter reached the footer button, so the scanner could **release quarantined stock** while the operator was still scanning where to put it. | Replaced with `ScanField`, which `preventDefault`s Enter |
| F3 | **S1** | **`buildScanIndex` was last-writer-wins on `Map.set`.** | Two products behind one barcode silently overwrote each other and the operator was handed the survivor with full confidence — breaking `resolveScan`'s own documented promise never to guess. Detected ambiguity *across* namespaces only, never within one. | Every bucket is a list; real collisions now report `ambiguous` — `lib/scan/resolveScan.ts` |
| F4 | **S1** | **A duplicated terminator committed the same code twice.** ReaderConfig offers `CRLF Character`, and a committed value is left *selected* rather than cleared. | Two receipt lines for one carton on Receive Stock. | 250 ms same-code guard on the keyboard path — `components/ui/ScanField.tsx` |
| F5 | **S2** | **CipherLab's default `Input Method` mode made keydown carry nothing** (`key:'Unidentified'`, `keyCode 229`); text arrives only via `input` events. | On the RS35's **factory-default configuration**, the Tab terminator and the no-suffix idle flush were both dead. | Character timing moved from `onKeyDown` to `onChange` (`noteArrival`) — deliberately not both, or a 140 ms human gap followed by a same-tick ~0 ms reading flips a slow typist to machine-speed |
| F6 | **S2** | **`ReceiveStockView` had no `ScanField` anywhere.** | The one place in the building where someone is certainly holding a barcode had no scan affordance. | Product / site-root / batch-barcode scanning via the pure `lib/scan/receiveScan.ts` |
| F7 | **S2** | **`BinPickerSheet`'s `ScanField` had no `onScan` prop at all**, and matched with `toLowerCase`. | A valid bin label read as "No active location with that code" — `String.trim()` leaves NUL and zero-width marks in place. The operator's eyes disagreed with the screen and nothing explained why. | `normalizeScan` on both sides, plus a real `onScan` |
| F8 | **S2** | **The mobile ☰ button sat on top of the warehouse selector.** `fixed top-4 left-4`, `md:hidden`, and no inventory page reserved space for it. | On the RS35 the site selector was unreachable on Stocktake, Putaway, Replenishment and Receive. | Explicit left clearance on the four page headers |
| F9 | **S2** | **No global wedge capture.** | A scan fired while focus was elsewhere was discarded in total silence — no beep, no error, no trace. The dominant desktop failure mode. | `lib/scan/useWedgeScanner.ts`, opt-in per surface, standing down on editable focus |
| F10 | **S3** | **Only `Enter` terminated a code.** | A scanner with a Tab suffix — or none — never committed. | Tab (gated on machine speed) plus a 120 ms quiet-time flush |
| F11 | **S3** | **No success feedback anywhere.** The scanner's own beep is identical for the right bin and the wrong one. | Operators learn to trust the beep and walk away from refused scans believing the work is recorded. | Accept/reject tone, border ring, and vibration — `lib/scan/scanFeedback.ts` |
| F12 | **S3** | **`ScanField.inputRef` was declared and never read.** | No refocus after a commit and no focus restore after the camera sheet closed, so every scan after the first needed a click — and the one after *that* went to the body and vanished. | The ref is used |
| F13 | **S3** | **`ReplenStopCard` never cleared the field on a refused scan**, unlike pick and putaway. | A rejected code stayed in the box and the next scan appended to it, producing a string that matched nothing and reported the wrong reason. | Clears on refusal; wrong destination is refused, wrong source recorded |
| F14 | **S3** | **The soft keyboard hid the stocktake Post bar.** AppShell is `h-screen overflow-hidden` and the bar is `sticky bottom-0`, so there was nothing to scroll. | On Android the Post button sat behind the keyboard and could only be reached by dismissing it. | `interactive-widget=resizes-content` on the viewport meta — `index.html` |
| F15 | **S3** | **`ScanField`'s input was 38 px and its camera button 44×38**, despite an in-file comment claiming a 44 px target for gloved thumbs. | The most-pressed control in the product was under-size on a touch device. | `min-h-[44px]` on both |
| F16 | **S3** | **The two "put it back" escape hatches were ~16 px tall** — the smallest targets on their cards, and what an operator reaches for when something has *already* gone wrong. | Hard to hit with a gloved thumb at a rack face. | `min-h-[44px]` — `PutawayStopCard`, `ReplenStopCard` |
| F17 | **S3** | **`PutawayStopCard`'s right-hand column was `shrink-0` with no width cap**, so `truncate` on its child could never fire. | At 360 px a long location name starved the product name — the one thing the walker is scanning the list for. | `max-w-[45%]` below `sm` |
| F18 | **S4** | **Demo had 0 product barcodes**, so `barcodeVariants` / GTIN folding had never met a real beam. | The most intricate branch in the resolver was untestable. | `npm run seed:barcodes:dev` mints check-digit-correct EAN-13/UPC-A |
| F19 | **S4** | **Diagnostics page measured the wrong gap** — `run.slowest` came off a clock the keydown handler also wrote, so it timed keydown→input latency. | A 90 ms inter-character delay reported as "worst gap 0 ms", i.e. the tool lied in exactly the case it exists to catch. | Separate character-only clock — the same two-clock trap `ScanField` already avoids |
| F20 | **S4** | **Clear-✕ misaligned after the touch-target fix.** `top-[38px]` was sized for the old 38 px input. | The ✕ sat near the bottom edge of the now-44 px field. | `top-[22px]`, tied to the input height, with the reason recorded |
| F21 | **S4** | **Product search lost its accessible name** when the bare `<input>` became a `ScanField`. | The field was unnamed to a screen reader. Caught by the existing suite. | `ariaLabel` prop on `ScanField` |

---

## How to add an entry

Append to **Open** with the next `O` number. When it ships, move the row to
**Fixed** with the next `F` number and record what the fix was — keep the
symptom text, because that is what makes it searchable later.

Do not delete rows. A defect that was fixed and came back is much easier to
recognise when the original entry is still there.
