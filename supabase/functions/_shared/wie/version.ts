// Warehouse Intelligence Engine — version stamp.
//
// Stamped into every PutawayExplanation so a stored recommendation can be traced
// back to the engine that produced it. Bump the minor version when scoring
// factors or their normalization change; bump the major on a breaking change to
// the explanation payload shape.

export const ENGINE_VERSION = 'wie-1.0.0'
