# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.vercel
coverage
.env
.env.local
.env.*.local

# Supabase CLI artifacts (project link metadata)
supabase/.temp/

# Playwright E2E artifacts
/test-results/
/playwright-report/
/playwright/.cache/
/blob-report/

# Superpowers brainstorming session artifacts (mockups, server state)
.superpowers/

# Demo database export (supabase/ops/export-demo.mjs). Holds the whole demo
# dataset and every storage object — never commit it, and copy it off this
# machine rather than relying on it living here.
/demo-export/
